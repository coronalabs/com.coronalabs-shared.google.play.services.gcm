Sources for the plugin `shared.google.play.services.gcm`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "shared.google.play.services.gcm" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
